//
//  AllertForCellName.swift
//  FinanceProject
//
//  Created by Anna Radoutska on 06.12.2021.
//

import Foundation
import UIKit


/*extension UIViewController{
    func AllertForCellCategory (label: UILabel, name: String, placeholder: String)
    {
        let alert = UIAlertController(title: name, message: nil, preferredStyle: .alert)
        let ok = UIAlertAction(title: "OK", style: .default, handler: (action) in
        let tfAlert = alert.textFields?.first
        guard let text = tfAlert?.text else {return}
        label.text = text
    }
    alert.addtextfield {(tfAlert ) in
    tfAlert
    
    
    }
}
*/
